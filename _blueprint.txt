function Pillars({ page }: { page: ServicePageData }) {
  const pillars = getPillarSections(page);

  return (
    <section className="bg-stone-50 py-20">
      <motion.div className="mx-auto max-w-8xl px-6 lg:px-16">
        <h2 className="font-display text-4xl font-semibold text-slate-900">
          Enterprise governance <span className="text-primary italic">as standard</span>
        </h2>
        <div className="mt-12 grid gap-4 sm:grid-cols-2">
          {pillars.map((s) => (
            <div key={s.title} className="rounded-xl border bg-white p-7">
              <span className="text-2xl">{PILLAR_ICONS[s.title] ?? '•'}</span>
              <p className="mt-3 text-xs font-bold uppercase tracking-widest text-primary">
                {s.title}
              </p>
              <p className="mt-3 text-sm leading-relaxed text-slate-600">
                {cleanText(s.body)}
              </p>
            </motion.div>
          ))}
        </div>
      </motion.div>
    </section>
  );
}
```

---

### 3. Why choose + key services (replace `WHY_ITEMS` / `SERVICES`)

Your data often packs **everything into one long `body`** on the last section (e.g. `Data Processing` at 34661). Two options:

**Option A (simple, robust):** One “Deep dive” section with full `body` as paragraphs (split on `. ` or render as-is with `whitespace-pre-line` after adding line breaks in `cleanText`).

**Option B (structured):** Parse markers in `body`:

- `🛡️ What Enterprise IT Directors Evaluate` → Why heading + following text
- `Key Progressive Web App Development Services` → Services heading
- Split on `. ` for bullet-like sentences

Pragmatic approach for v1:

```tsx
function ContentBlock({ page }: { page: ServicePageData }) {
  const long = getLongFormSection(page);
  if (!long?.body) return null;

  return (
    <section className="bg-white py-20">
      <motion.div className="mx-auto max-w-3xl px-6 lg:px-16">
        <h2 className="font-display text-3xl font-semibold text-slate-900">
          Why {page.subtitle.replace(/^FOR /i, '')} choose ClickMasters
        </h2>
        <div className="prose prose-slate mt-8 max-w-none text-justify leading-relaxed">
          {cleanText(long.body)}
        </div>
      </motion.div>
    </section>
  );
}
```

Later you can refine parsing; this already shows **all** persona copy from the file.

If you want to keep **3 service cards**, take `sections` that have `bullets` or add a second pass on `long.body` to extract 3 chunks by keywords.

---

### 4. Pricing (replace `TIERS` + duplicate `Pricing()`)

```tsx
<PricingSection
  serviceName={page.title}
  pricingTiers={toPricingTiers(page.pricing)}
/>
```

Remove unused local `Pricing()` and hardcoded `TIERS`.

Shows your 5 tiers: `$4,000-$8,000` … `$5,000-$15,000/mo`.

---

### 5. FAQ (replace `FAQS`)

```tsx
<FAQSection faqs={page.faqs} />
```

Already matches `{ question, answer }`.

---

### 6. CTA (replace `CTASection()` / default `ChecklistCTAHero`)

```tsx
const ctaRow = getPricingCta(page.pricing);
const ctaText = page.callToAction?.text ?? ctaRow?.investment ?? 'Book a consultation';

<ChecklistCTAHero
  title={ctaRow?.investment ?? 'Ready to get started?'}
  description={ctaRow?.timeline ?? cleanText(ctaText)}
  buttons={[
    { text: 'Book consultation', href: '/contact-us', variant: 'primary', icon: 'calendar' },
    { text: 'Contact us', href: '/contact-us', variant: 'outline', icon: 'arrow' },
  ]}
/>
```

---

### 7. Internal links (optional)

```tsx
{page.internalLinks?.length > 0 && (
  <section className="mx-auto max-w-8xl px-6 py-12">
    <h3 className="text-lg font-semibold">Related pages</h3>
    <ul className="mt-4 flex flex-wrap gap-4">
      {page.internalLinks.map((link, i) => (
        <li key={i}>
          <Link href={link.href ?? link.url ?? '#'} className="text-primary hover:underline">
            {link.label ?? link.title}
          </Link>
        </li>
      ))}
    </ul>
  </section>
)}
```

---

## Server `page.tsx` skeleton

```tsx
import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { getPersonaPage, getAllPersonaSlugs } from '@/src/lib/persona-utils';
import { getServicePage } from '@/lib/service-pages';
import PersonaPageClient from './PersonaPageClient';

type Props = {
  params: Promise<{ slug: string; service: string; 'persona-based': string }>;
};

export function generateStaticParams() {
  return getAllPersonaSlugs().map((personaSlug) => {
    const base = personaSlug.replace(/-for-[^-]+.*$/, ''); // rough service base
    const svc = getServicePage(base);
    return {
      slug: svc?.categorySlug ?? 'services',
      service: base,
      'persona-based': personaSlug,
    };
  });
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { 'persona-based': personaSlug } = await params;
  const page = getPersonaPage(personaSlug);
  if (!page) return { title: 'Persona' };
  return {
    title: page.metaTitle,
    description: page.metaDescription,
  };
}

export default async function PersonaPage({ params }: Props) {
  const resolved = await params;
  const page = getPersonaPage(resolved['persona-based']);
  if (!page) notFound();

  return <PersonaPageClient page={page} breadcrumbs={resolved} />;
}
```

---

## Client root (wire all sections)

```tsx
'use client';

export default function PersonaPageClient({
  page,
  breadcrumbs,
}: {
  page: ServicePageData;
  breadcrumbs: { slug: string; service: string };
}) {
  return (
    <div className="min-h-screen bg-white font-sans text-slate-900 antialiased">
      <PersonaBreadcrumb {...breadcrumbs} title={page.title} />
      <Hero page={page} />
      <Pillars page={page} />
      <ContentBlock page={page} />      {/* long-form body */}
      <PricingSection
        serviceName={page.title}
        pricingTiers={toPricingTiers(page.pricing)}
      />
      <FAQSection faqs={page.faqs} />
      <PersonaCta page={page} />
    </div>
  );
}
```

Delete: `PILLARS`, `WHY_ITEMS`, `SERVICES`, `TIERS`, `FAQS`, `CybersecurityEnterprisePage`, `FONT_URL` injection, and the unused `Pricing()` / `FAQ()` / `CTASection()` if you use shared components.

---

## What to remove from the current file

| Remove | Replace with |
|--------|----------------|
| All `const PILLARS / WHY_ITEMS / SERVICES / TIERS / FAQS` | `page.*` |
| `Hero()` with no props | `Hero({ page })` |
| `serviceName="Cybersecurity"` | `page.title` |
| Inline `T` colors (optional) | Tailwind: `text-primary`, `bg-stone-50`, `font-display` |

---

## Data quirks to handle

1. **Duplicate slugs** (`for-product-managers`) — use full slugs in URLs only (`…-for-enterprise-it-directors`).
2. **Huge `body` strings** — use `ContentBlock` or split later; don’t try to fit all into 3 hardcoded cards without parsing.
3. **Empty `Features.body`** — OK; rely on `bullets` only (PWA case).
4. **49k-line import** — server loads data; client receives one `page` object (keeps bundle smaller).

---

## Summary

| Page section | `persona-based.ts` source |
|--------------|---------------------------|
| Hero | `title`, `subtitle`, `sections[Features].bullets` / `topBullets` |
| Pillars | `sections` (SOC 2, Change Management, Data Processing, etc.) |
| Long content | Last / long `section.body` |
| Pricing | `pricing[]` (filter CTA row) |
| FAQ | `faqs[]` |
| CTA | `callToAction` + last `pricing` row |
| SEO | `metaTitle`, `metaDescription` |

---

I'm in **Ask mode** and cannot edit the project. Switch to **Agent mode** and ask to “make persona page fully dynamic from persona-based.ts” — I can then add `persona-utils.ts`, split server/client files, and wire every section with Tailwind.

[REDACTED]